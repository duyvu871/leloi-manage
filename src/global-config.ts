export const API_ENDPOINT = process.env.NEXT_PUBLIC_API_HOST;
export const PREFIX = 'api';
export const PREFIX_VERSION = 'v1';
export const API_URL = `${API_ENDPOINT}/${PREFIX}/${PREFIX_VERSION}`;
export const BASE_HOST = process.env.NEXT_PUBLIC_BASE_HOST;

export const useMock = true;
